# Server-side only — set these in Netlify (Site settings → Environment variables),
# never in client code.
GEMINI_API_KEY=
# Optional: chat-question stats. Omit to disable logging.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
